(use-package osx-lib
  :config
  (setq osx-lib-say-rate 200)
  (setq osx-lib-say-voice "Jamie (Premium)")

  ;; NOTE issue with this is that it actually plays the text all the
  ;; way through.  No way to interrupt it... should implement my own
  ;; function to display in a side buffer at top with interactive mode
  ;; turned on
  )




