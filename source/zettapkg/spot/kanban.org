
* TODO spot--playlist-selected vs spot--selected-playlist-id

* TODO fix issue with mode line check when there's nothing playing

* TODO Make API requests async (there is an async variant of the url-retrieve-[sync/aysnc]hronously)
:LOGBOOK:
- State "TODO"       from              [2025-06-19 Thu 19:54:57]
:END:
- Would obviate use of run with timer?  I don't think so because I would still need the delay
* DONE projectify
:LOGBOOK:
- State "DONE"       from "TODO"       [2025-06-19 Thu 19:54:24]
:END:
* DONE delete -- if this is used it could be using an expired token
:LOGBOOK:
- State "DONE"       from "TODO"       [2025-06-19 Thu 19:55:03]
:END:
* TODO for sources (like list playlist, playlist tracks) which return a static potentially large number of results, need to implement paging or something to pull all tracks
- given how consult works, probabaly better to retrieve all results
* TODO limit the limit value to 50 (eg if >50, make 50)
* TODO how to sort (this is a core consult thing, try there first) workaround is to add marginalia
* TODO getting prompted for username when trying to add playlist?
* TODO fix timer when nothing is playing -- related to negative 'next
excerpt from list-timers
-1d 23h 46m 51.7s        30.0s spot--check-for-modeline-update
* TODO issue with query not returning anythign -- looks like it freezes at the level of this function: spot--search-items
* TODO history keybinding in consult search not working?
* TODO history needs to work 
* TODO add function to 'log in' when executing first request
* TODO acting on (not 'selecting') candidates fails to put them into the sort history for recency purposes?
* TODO more generic play function with context, type
* TODO processing when certain categories are empty
* TODO test query params with white space
* TODO disable sort?  is this getting rid of 'most relevant' -- important to think critically about this in the abstract -- should I defer sorting to the backend?  Is there a way to re-sort on the fly from the minibuffer? Double check sorting from spotify
* TODO is it possible to pause the requesting by the backend while I edit a potentially complex command?
* TODO more efficient to set the modeline things from within the function that plays the track?  instead of starting async to submit an additional API request?
* Notes

;; NOTE user interaction with the API can interfere... any playback
;; controlled via spot- should run update modeline lighters after
;; playback is complete -- could be sync issues

;; NOTE ;; ligthers are updated when packages loads
* make value of spot--timer-started a timer
* NOTE delay prevents this from occurring before the update has
     ;; taken place in the player... using run-with-timer prevents
     ;; making emacs sleep
* ;;TODO alignment and faces, holding off for now.  marginalia--fields
;;is useful for this

* ;; TODO remove redundant information like name

* marginalia:       ;; TODO express in minutes.seconds

*          ;; TODO allow for things like playlist context when selecting ;; a track from a playlist (ditto for albums) -- would ;; require settinga selection-context variable as this data ;; wouldn't get read from the item itself, but rather ;; inferred by the user's previous menu selection... check if ;; it's already included

* ;; TODO (embark) how can keymap hierarchy be used?  is this even necessary?





