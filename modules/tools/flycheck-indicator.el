;;; flycheck-indicator.el --- Configure flycheck-indicator -*- lexical-binding: t; -*-

(use-package flycheck-indicator
  ;; flycheck indicator mode turns normal flycheck into a nice
  ;; indicator
  :config
  (flycheck-indicator-mode 1)
  )
;;; flycheck-indicator.el ends here
